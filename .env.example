# Public (safe in the browser)
NEXT_PUBLIC_SUPABASE_URL=https://oeunlirtgvqwftcqludy.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Server-only — for sending employee invitation emails (Supabase Auth admin).
# NEVER prefix with NEXT_PUBLIC_. Never expose to the client. Optional: without
# it, invites still work via the "Copy invite link" fallback (no email sent).
SUPABASE_SERVICE_ROLE_KEY=
APP_URL=https://confidel-ops.vercel.app

# Optional error monitoring (see DEPLOY.md). DSNs are public; safe to set.
# SENTRY_DSN=
# NEXT_PUBLIC_SENTRY_DSN=
